# Development — Flask dev server + volume mount
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
EXPOSE 5001
ENV FLASK_ENV=development
CMD ["python", "predict_service.py"]
